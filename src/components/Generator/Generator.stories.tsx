import { Meta } from '@storybook/react';

import { Generator } from './Generator';

export default {
  title: 'Components/Generator',
} as Meta<typeof Generator>;

export const _Generator = () => <Generator />;
